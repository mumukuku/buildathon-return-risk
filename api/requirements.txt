# Dependencies for the DEPLOYED API function only.
# Deliberately excludes: shap, matplotlib, networkx.
# - shap pulls in numba (bundles LLVM via llvmlite, ~150-250MB) + matplotlib's
#   font/backend stack, even though the API doesn't need shap's own code --
#   see api/lib/scoring.py for the explanation of why (native XGBoost
#   pred_contribs replaces it, same underlying algorithm, zero extra deps).
# - matplotlib and networkx are used only by the offline training scripts in
#   model/, never at request-serving time.
#
# This file lives in api/ so Vercel's Python builder picks it up for the
# api/index.py function instead of the heavier root requirements.txt used
# for local training/notebook work.

fastapi==0.141.1
pydantic==2.9.2
joblib==1.5.3
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
xgboost==3.4.1
